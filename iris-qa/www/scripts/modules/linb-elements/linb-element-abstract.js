//@TODO: shared part of linb element initialization for Operator-Display and Hold-Screen elements (and may be others)

// all linb elements should have unique names linb.Com.load("App" <- this should be generated, or specified name